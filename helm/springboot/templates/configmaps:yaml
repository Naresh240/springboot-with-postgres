apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "postgres.fullname" . }}
  labels:
    app: {{ template "postgres.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"  
data:
  {{ if .Values.configmaps.postgress-host }}
  postgress-host: {{ .Values.configmaps.postgress-host | b64enc | quote }}
  {{- end }}
